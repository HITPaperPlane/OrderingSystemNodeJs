<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Homepage - Golden Bowl</title>
</head>
<body>
    {{> navbar loginUser=loginUser}}
    <section>
        <div class="container-fluid pb-5">
            {{> userBar}}

            <div class="container userSection">
                <div class="row">
                    <div class="col-md-3 text-center">
                        <!-- Display user avatar dynamically based on availability -->
                        {{#if loginUser.photo}}
                        <img class="img-fluid" style="border-radius:50%; max-width:180px;" src="/static/userImages/{{loginUser.photo}}" alt="User Avatar">
                        {{else}}
                        <img class="img-fluid" style="border-radius:50%; max-width:180px;" src="/static/image/review/review-1.jpg" alt="Default Avatar">
                        {{/if}}
                    </div>
                    <div class="col-md-9">
                        <div class="card py-3 px-3">
                            <h4>Profile Information</h4>
                            <hr>
                            <div class="row">
                                <div class="col-md-4 text-muted">Name: </div>
                                <div class="col-md-8">{{loginUser.name}}</div>
                            </div>
                            <div class="row">
                                <div class="col-md-4 text-muted">Email: </div>
                                <div class="col-md-8">{{loginUser.email}}</div>
                            </div>
                            <div class="row">
                                <div class="col-md-4 text-muted">Phone: </div>
                                <div class="col-md-8">{{loginUser.phone}}</div>
                            </div>
                            <div class="row">
                                <div class="col-md-4 text-muted">Address: </div>
                                <div class="col-md-8">{{loginUser.address}}</div>
                            </div>
                            <div class="row">
                                <div class="col-md-4 text-muted">Account Type: </div>
                                <div class="col-md-8">{{loginUser.type}}</div>
                            </div>

                            <div class="mt-3 text-end">
                                <a class="btn btn-primary" href="/user/editProfile">Edit Profile</a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

    </section>
</body>
</html>
