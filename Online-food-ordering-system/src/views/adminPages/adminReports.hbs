<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <title>Report - Admin</title>
</head>
<body>
  {{> navbar loginUser=loginUser}}
  
  <section>
    <div class="container-fluid">
      <div class="row">
        <div class="col-md-3">
          {{> sidebar}}
        </div>
        <div class="col-md-9">
          <div class="card m-3 p-3">
            <div class="d-flex align-items-center mb-3">
              <h4 class="flex-grow-1">Report</h4>
              <a href="/dashboard" class="btn btn-sm btn-secondary">Back</a>
            </div>
            <hr>

            <p>Report period: 
              {{#if (eq reportData.period 'week')}}Past 7 days
              {{else}}Past 30 days
              {{/if}}
            </p>
            <p>Amount: {{reportData.totalOrders}} 单</p>
            <p>Total sales: ${{reportData.totalSales}}</p>
            <p>Most popular: {{reportData.popularDish}} (总销量 {{reportData.popularDishQuantity}} 份)</p>

            <div class="mt-3">
              <a href="/admin/reports?period=week" class="btn btn-info btn-sm">Weekly</a>
              <a href="/admin/reports?period=month" class="btn btn-info btn-sm">Monthly</a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

</body>
</html>
